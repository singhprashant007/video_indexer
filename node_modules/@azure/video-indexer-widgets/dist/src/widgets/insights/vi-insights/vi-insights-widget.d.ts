import { BaseWidget } from '../../base';
import { IWidgetOptions } from '../../common/options';
import { IInsightsWidgetConfig } from './definitions/vi-widget-config.definitions';
export declare class InsightsWidget extends BaseWidget {
    private config;
    constructor(elementID: string, options: IWidgetOptions, config: IInsightsWidgetConfig);
    protected get iframeUrl(): string;
    protected communicate(event: MessageEvent): void;
}
