import { AccountLocation, IInsightsConfig, IInsightsSortedBy, PresetType } from '../../definitions/insights-config.definitions';
import { IAudioEffect, IBlock, IBrand, ICustomItemElement, IEmotion, IFramePattern, IKeyword, ILabel, INamedLocations, INamedPeople, IOcr, ISentiment, ISpeaker, ITopic, ITranscriptLine } from './video-insights.definitions';
export type VIInsightsDataType = ITranscriptLine | IOcr | IKeyword | ITopic | ILabel | IBrand | INamedPeople | INamedLocations | ISentiment | IEmotion | IBlock | IFramePattern | IAudioEffect | ISpeaker;
export type insightsType = 'capsule' | 'color-map';
export type VIInsightsKey = 'faces' | 'animatedCharacters' | 'keywords' | 'labels' | 'sentiments' | 'emotions' | 'topics' | 'shots' | 'keyframes' | 'transcript' | 'ocr' | 'speakers' | 'scenes' | 'brands' | 'namedPeople' | 'namedLocations' | 'audioEffects' | 'blocks' | 'framePatterns' | 'observedPeople';
export type VIRawInsightsKey = 'keywords' | 'labels' | 'sentiments' | 'emotions' | 'topics' | 'transcript' | 'ocr' | 'speakers' | 'brands' | 'namedPeople' | 'namedLocations' | 'audioEffects' | 'blocks' | 'framePatterns';
export interface ICustomData {
    /**
     * Custom insights data title
     */
    title: string;
    /**
     * Custom insights data key
     */
    key: string;
    /**
     * Define which presets controls this insights
     */
    presets: PresetType[];
    /**
     * Define insights type (capsule / color map)
     */
    type: insightsType;
    /**
     * Custom data items.
     */
    items: ICustomItemElement[];
    /**
     * Sorted by
     */
    sortedBy?: IInsightsSortedBy;
}
export interface IVRawIInsightsData {
    /**
     * Video indexer insights component name
     */
    rawInsightsKey: VIRawInsightsKey;
    /**
     * Insights data items.
     */
    insightsItems: VIInsightsDataType[];
}
export interface ICustomInsightsWidgetConfig extends IInsightsConfig {
    /**
     * Video indexer account Id.
     * Should be provided if you want to extract data from VI
     */
    accountId?: string;
    /**
     * Embedded video ID
     * Should be provided if you want to extract data from VI
     */
    videoId?: string;
    /**
     * Embedded video access token.
     * Should be provided if you want to extract data from private video in VI
     */
    accessToken?: string;
    /**
     * Location indicates account's location.
     * Should be provided if you want to extract data from VI
     */
    location?: AccountLocation;
    /**
     * Video duration
     */
    duration: number;
    /**
     * Video indexer insights component key list, each insight from this list will be taken from VI.
     */
    viInsightsKeys: VIInsightsKey[];
    /**
     * Video indexer insights raw data. Use VI insights component with your own data
     */
    rawInsightsData: IVRawIInsightsData[];
    /**
     * Custom insights data. Provide your own insights - support both capsules and color map.
     */
    customData: ICustomData[];
}
/**
 * Widget messages
 */
export declare enum CustomInsightsWidgetMessage {
    INSIGHTS_WIDGET_LOADED = "INSIGHTS_WIDGET_LOADED",
    CUSTOM_INSIGHTS_WIDGET_INIT = "CUSTOM_INSIGHTS_WIDGET_INIT"
}
